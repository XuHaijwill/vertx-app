# ============================================================
# application.yml — 共享默认配置
# profile 特定配置见 application-DEV/UAT/PROD.yml
# ============================================================

app:
  name: vertx-app
  http-port: 8888

# 数据库配置（公共默认值，profile 文件可覆盖）
db:
  host: localhost
  port: 5432
  database: vertx
  user: postgres
  password: 123456
  pool-size: 10
  ssl: false
